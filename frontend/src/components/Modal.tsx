import React from "react";

interface Props {
  openModal: boolean;
  handleModal: (x: any) => void;
}
const Modal = () => {
  return (
    <div>
      <div>Coming soon...</div>
    </div>
  );
};

export default Modal;
